# QA

## MVP acceptance criteria
- A round can start from a cold server state.
- At least one task can be completed and counted.
- The HUD reflects round state and progress.
- A blackout event can fire without breaking the round.
- End-of-round payout is visible.
- Smoke script can detect core services/modules without crashing.

## Sprint 4 final screenshot recheck
- status: Ready
- what was checked:
  - `project/docs/RUNTIME-EVIDENCE.md` final appended Sprint 4 screenshot evidence only
  - success results wording (`Success.png`)
  - failure results wording (`Failure.png`)
  - `Saved Cash` landing after results (`Saved_Cash.png`)
  - late-join readability (`Late_Join.png`)
  - phone-sized HUD/results readability (`Phone.png`)
- result:
  - The remaining Sprint 4 launch-facing client-visible evidence blocker is cleared.
  - The appended evidence set records all five required artifacts as Pass.
  - No new blocker was found in this ultra-narrow recheck.
- risks:
  - This call is intentionally limited to the final appended client-visible evidence set and does not reopen broader Sprint 4 scope.
- next fixes:
  - none for this blocker

## Bug report template
- Area:
- Build / commit:
- Setup:
- Steps:
- Expected:
- Actual:
- Severity:
- Notes:
